import {useState} from 'react';import {Navigate} from 'react-router-dom';import {ChefHat,Eye,EyeOff} from 'lucide-react';import {useAuth} from '../store/auth';import {Button,Field} from '../components/ui';import {messageOf} from '../services/api';
export default function Login(){const {user,login}=useAuth();const [email,setEmail]=useState('admin@tableflow.local'),[password,setPassword]=useState('Demo@123'),[show,setShow]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState('');if(user)return <Navigate to="/dashboard" replace/>;const submit=async(e:any)=>{e.preventDefault();setLoading(true);setError('');try{await login(email,password)}catch(e){setError(messageOf(e))}finally{setLoading(false)}};return <div className="grid min-h-screen bg-slate-950 lg:grid-cols-2"><section className="relative hidden overflow-hidden p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,rgba(22,168,120,.35),transparent_35%),radial-gradient(circle_at_80%_80%,rgba(56,189,248,.2),transparent_32%)]"/><div className="relative flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-brand-500"><ChefHat/></span><span className="text-xl font-bold">TableFlow</span></div><div className="relative max-w-xl"><p className="mb-5 text-sm font-semibold uppercase tracking-[.25em] text-brand-500">Restaurant operations, in rhythm</p><h1 className="text-5xl font-bold leading-tight">From first tap to final plate, keep every team in sync.</h1><p className="mt-5 text-lg leading-relaxed text-slate-400">Fast service at the counter. Calm coordination in the kitchen. Clear numbers in the office.</p></div><p className="relative text-sm text-slate-600">© 2026 TableFlow Systems</p></section><section className="flex items-center justify-center bg-slate-50 p-5"><div className="w-full max-w-md rounded-3xl bg-white p-7 shadow-2xl shadow-slate-950/10 md:p-10"><div className="mb-8 lg:hidden"><span className="text-xl font-bold">TableFlow</span></div><h2 className="text-3xl font-bold">Welcome back</h2><p className="mt-2 text-slate-500">Sign in to start your shift.</p><form onSubmit={submit} className="mt-8 space-y-5"><Field label="Email address" type="email" value={email} onChange={(e:any)=>setEmail(e.target.value)} required/><label className="block text-sm font-medium text-slate-700">Password<div className="relative mt-1.5"><input className="w-full rounded-xl border border-slate-200 px-3.5 py-2.5 pr-11 outline-none focus:border-brand-500 focus:ring-2 focus:ring-brand-100" type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-2.5 text-slate-400">{show?<EyeOff size={19}/>:<Eye size={19}/>}</button></div></label>{error&&<p className="rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<Button loading={loading} className="w-full">Sign in</Button></form><div className="mt-7 rounded-xl bg-slate-50 p-4 text-xs leading-5 text-slate-500"><strong className="text-slate-700">Demo account</strong><br/>admin@tableflow.local · Demo@123</div></div></section></div>}
