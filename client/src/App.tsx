import {Navigate,Route,Routes} from 'react-router-dom';import {useEffect} from 'react';import {useAuth} from './store/auth';import {AppShell} from './components/layout/AppShell';import Login from './pages/Login';import Dashboard from './pages/Dashboard';import POS from './pages/POS';import Orders from './pages/Orders';import Tables from './pages/Tables';import KDS from './pages/KDS';import ResourcePage from './pages/ResourcePage';import Reports from './pages/Reports';import Settings from './pages/Settings';import {LoaderCircle} from 'lucide-react';import {Toasts} from './components/ui';
function Guard(){const {user,loading}=useAuth();if(loading)return <div className="grid min-h-screen place-items-center bg-[#0b0b0d]"><LoaderCircle className="animate-spin text-zinc-100"/></div>;return user?<AppShell/>:<Navigate to="/login" replace/>}
const resources:any={menu:{endpoint:'menu-items',title:'Menu items'},inventory:{endpoint:'inventory',title:'Inventory'},recipes:{endpoint:'recipes',title:'Recipes'},customers:{endpoint:'customers',title:'Customers'},suppliers:{endpoint:'suppliers',title:'Suppliers'},purchases:{endpoint:'purchases',title:'Purchases'},expenses:{endpoint:'expenses',title:'Expenses'},accounting:{endpoint:'reports/accounting',title:'Accounting'},staff:{endpoint:'staff',title:'Staff'}};
export default function App(){const restore=useAuth(s=>s.restore);useEffect(()=>{restore()},[restore]);return <><Routes><Route path="/login" element={<Login/>}/><Route element={<Guard/>}><Route index element={<Navigate to="/dashboard" replace/>}/><Route path="/dashboard" element={<Dashboard/>}/><Route path="/pos" element={<POS/>}/><Route path="/orders" element={<Orders/>}/><Route path="/tables" element={<Tables/>}/><Route path="/kds" element={<KDS/>}/>{Object.entries(resources).map(([path,props])=><Route key={path} path={`/${path}`} element={<ResourcePage {...props as any}/>}/>) }<Route path="/reports" element={<Reports/>}/><Route path="/settings" element={<Settings/>}/></Route><Route path="*" element={<Navigate to="/dashboard" replace/>}/></Routes><Toasts/></>}
